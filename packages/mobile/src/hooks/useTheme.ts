import Font from '@mobile/theme/fonts';
import Layout from '@mobile/theme/layout';
import Spacing from '@mobile/theme/spacing';

export const useTheme = () => {
  return {
    Font,
    Layout,
    Spacing,
  };
};
